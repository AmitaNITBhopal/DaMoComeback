<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_shareScreenShot"
        android:icon="@drawable/ic_icons8_share"
        android:title="@string/abc"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_ShareApp"
        android:title="Share App"
        app:showAsAction="never" />

   <!-- <item
        android:id="@+id/action_addBackground"
        android:title="Add Background"
        app:showAsAction="never" />
-->
    <!--<item
        android:id="@+id/action_Alarm"
        android:title="Alarm"
        app:showAsAction="never" />-->

    <!-- Settings, should always be in the overflow -->
  <!--  <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        app:showAsAction="never"/>-->
</menu>